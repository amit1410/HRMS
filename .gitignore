# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
*.user

# Rider / VS
.idea/
.vs/

# Logs
logs/
*.log

# Local dev SQLite database (schema is Code-First; never committed)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.bak
*.backup
*.dump
*.dmp
*.mdf
*.ldf

# Frontend
node_modules/
dist/
coverage/

# Local environment and secrets (keep example templates)
.env
.env.*
!.env.example

# Local runtime artifacts and generated checks
_buildcheck/
*.out
*.err
*.pid
*.tmp
*.cache
api-*.txt
login*.json
resp.json
token*.txt
e2e-token.txt
start-backend.ps1
start-frontend.ps1

# Local tool state. `~/` is a stray directory created when a tool wrote to a literal "~" instead of
# expanding it to the home directory; it contains an API key, so it must never be committed.
.claude/settings.local.json
~/

# Browser acceptance/runtime state (may contain credentials or tokens)
tools/phase3b-browser-state.credentials.txt
tools/phase3b-browser-state.json
tools/phase3b-browser-state.manifest.json
tools/phase3b-browser-state.processes.json
tools/phase3b-resume-attempt-*/
tools/phase3b-tls-original-config-*.json

# Temporary validation output
.phase*-obj/
.phase*-build/
.phase*-restore-diagnostic/
.recovery-*-obj*/
artifacts/
allocated-single-test.txt
mysql-provider-test.txt
provider-routing-test.txt
--project
0

# OS
Thumbs.db
.DS_Store
