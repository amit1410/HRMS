# Local development. Committed on purpose: it holds no secrets, and `npm run dev` should work
# against a locally running API with no extra setup. Anything sensitive belongs in `.env.local`,
# which is gitignored.
VITE_API_BASE_URL=http://localhost:5080
